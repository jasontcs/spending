import 'package:flutter/material.dart';

class RecordEditPage extends StatelessWidget {
  const RecordEditPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Record Edit')),
    );
  }
}
